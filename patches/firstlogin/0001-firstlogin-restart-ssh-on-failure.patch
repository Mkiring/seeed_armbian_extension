diff --git a/packages/bsp/common/usr/lib/armbian/armbian-firstlogin b/packages/bsp/common/usr/lib/armbian/armbian-firstlogin
--- a/packages/bsp/common/usr/lib/armbian/armbian-firstlogin
+++ b/packages/bsp/common/usr/lib/armbian/armbian-firstlogin
@@ -1010,9 +1010,9 @@
 		rm -f "${_f}.tmp"
 		exit 1
 	fi
-	# Reload keeps the active SSH session alive, so the marker can remain until
-	# every remaining firstlogin action completes.
-	systemctl reload ssh.service || exit 1
+	# Restart ssh.service; if it fails do not abort firstlogin, otherwise
+	# /root/.not_logged_in_yet is left behind and the setup loops on reboot.
+	systemctl restart ssh.service || logger -t armbian-firstlogin "Failed to restart ssh.service"
 
 	# rpardini: hacks per-dm, very much legacy stuff that works by a miracle
 	if [[ "${desktop_dm}" == "lightdm" ]] && [ -n "$RealName" ]; then
